name: BayesTrends
channels:
  - conda-forge
dependencies:
  - arviz>=0.13.0 # Install with PiP if not available on Anaconda
  - cartopy
  - cmdstanpy
  - ipywidgets
  - jupyterlab
  - matplotlib
  - numpy
  - pandas
  - patsy
  - requests
  - scipy
  - tqdm
  - watermark
  - xarray

